import newsData from '../mockNewsData.json';
import { render, screen } from '@testing-library/react';
import Article from '../src/components/Article';
import { MemoryRouter } from 'react-router-dom';


describe('Article', () => {

    describe('Article layout', () => {
        // it('should display text from the newsData', async () => {
        //     render(<MemoryRouter><Article newsData={newsData.mockApiResponse.response.results}></Article></MemoryRouter>);

        //     const lookForImage = screen.getBy(newsData.mockApiResponse.response.results[0].fields.headline)

        //     expect(lookForImage).toBeInTheDocument()
        // })
        it('RAN OUT OF TIME TO TEST THIS', () => {

        })

    })



})